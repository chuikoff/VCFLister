# VCFLister

**VCFLister** — плагин Lister (`.wlx64`) для [Total Commander](https://www.ghisler.com/), позволяющий удобно просматривать контакты из файлов формата **VCF/vCard**.

## Возможности

- Поддержка формата **vCard 3.0** (в т.ч. `itemN.TEL`, `itemN.EMAIL`).
- Отображение списка контактов (слева) и подробной информации по выделенному контакту (справа).
- Поддержка фото (через GDI+), автоматическое масштабирование, DPI-aware.
- Отображаются все основные поля:
  - Имя (FN, N)
  - Телефоны (с типами HOME/WORK/MOBILE и т.д.)
  - Email-адреса
  - Организация (ORG), должность (TITLE)
  - Дата рождения (BDAY)
  - Адреса (ADR)
  - Примечания (NOTE)
  - Ссылки (URL)
- Поддержка поиска по всем полям карточки (Ctrl+F / F3 / Shift+F3), в т.ч. NICKNAME/IMPP/X-*; Match case / whole word.
- Полная поддержка сложных vCard (Apple, OPPO и др.): X-ABLABEL, социальные профили, связанные имена, альтернативные даты, Android Custom Events и т.д. (метки приклеиваются к полям, без сырого "Label: _$!<...>!$_").
- Показ всех контактов, включая пустые (отмечаются как "(пустая карточка)").
- Фото по левому верхнему краю.
- Стабильное переключение плагинов и режимов просмотра (даже после клика в данные).
- Улучшенная очистка полей (N, ADR без лишних `;`), надёжная работа с фото и фокусом.

## Установка

1. Скопируйте файл плагина в папку `Plugins\Wlx` Total Commander (или любую подпапку).
2. В Total Commander откройте любой .vcf файл — плагин подключится автоматически.
3. Для 64-битной версии Total Commander используйте `VCFLister.wlx64`.
4. Для 32-битной версии — `VCFLister.wlx`.

## Управление

- **Фильтр** над списком — мгновенный поиск по полям контакта.
- **📷 фото** — показать только контакты с фото.
- **Разделитель** списка и карточки — перетаскивается мышью (ширина сохраняется в ini TC).
- **ПКМ** по списку: копировать имя / телефон / email / карточку.
- **ПКМ** по тексту: копировать выделение / значение строки / всё.
- **Ctrl+C** — копировать выделение, значение строки или имя контакта.
- **Поиск** (Ctrl+F / F3): совпадения подсвечиваются в списке, в статусбаре — «Найдено: N (p/N)» (счётчик следует за выбором).
- Наведение на длинное имя — tooltip с полным именем.
- **Один скролл** правой карточки — прокручивает текст и фото.
- Порядок карточки: **фото сверху**, затем поля (превью с лимитом размера).
- В карточке **значения полей жирным** (подписи обычным), блок **NOTE** отделён.
- Тёмная тема следует **режиму TC** (`cm_SwitchDarkMode`), а не теме Windows.

## Настройки (в wincmd.ini, секция `[VCFLister]`)

| Ключ | Значение | По умолчанию |
|------|----------|--------------|
| `ListWidth` | ширина списка (логич. px @96dpi) | 300 |
| `LoadPhotoUrl` | `1` — загружать PHOTO по http(s) | `0` (выкл.) |
| `Dark` | `0` светлая / `1` тёмная / `2` как в TC | `2` |

## Сборка

См. `compile_instructions.txt`.

## Лицензия

См. LICENSE.txt.

