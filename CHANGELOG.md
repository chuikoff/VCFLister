## [2.5.0] – 2026-08-18

### Changed
- Карточка строится из **одного разбора** (`Contact.fields` / `BuildFromContact`), без повторного parse raw-блока.
- Поиск: по полям по отдельности (без склейки огромной `hay`), плюс `fields` / `androidCustoms` (NICKNAME, IMPP, X-*, …).
- Уважается **Match case** (`lcs_matchcase`); `wrap=false` сканирует до края списка.
- `A2W` на `std::wstring` (без `malloc`/`free`).

### Fixed
- Статусбар **«Найдено: p/N»**: `matchPos` обновляется при клике/стрелках (`SetSelectionAndReveal`), не только в F3.
- Tooltip списка: тот же hit-test, что у клика (фильтр + `visibleIdx`).
- `PhotoWndProc`: безопасный `ViewStateFromHwnd` (`IsWindow` + null).
- Поиск: корректная длина после `LowerInvariant` (напр. `ß` → `ss`).
- Win32: `TargetExt=.wlx` (без MSB8012 TargetPath/`.dll`).

### Cleanup
- Удалён мёртвый SFINAE (`addNoteImpl` / `has_notes`), пустой `vcf_view_theme.hpp`, `*.orig`, `original_*.txt`, `fix*.ps1`, `RCa14392`.
- `.gitignore`: `*.orig`, `original_*.txt`, `RCa*`.

## [2.4.7] – 2026-08-01

### Fixed
- **#19** «Copy line value» всегда копирует **полное** значение строки (не выделение); копирование выделения без потери символа (RichEdit `EM_GETTEXTRANGE`).
- **#20** Тёмная тема: подпись **📷 photo** — светлый цвет (`WM_CTLCOLORBTN` + `SetWindowTheme`).

## [2.4.6] – 2026-07-31

### Changed
- **#18** Карточка как **таблица**: подпись | значение (tab + цвет), без bold.
- **NOTE** — отдельный блок `── Заметка ──` с переносами строк (`\n` / `\t`).

### Fixed
- ПКМ по карточке **после выделения** — краш TC: RichEdit `EM_CHARFROMPOS` требует `POINTL*`, не `MAKELPARAM`.
- Зависание ПКМ-меню под Lister: `PostMessage` + безопасный `TrackPopupMenu`, без reentrancy.
- Выделение мышью: фокус не уводится с текста во время drag-select.

## [2.4.5] – 2026-07-31

### Added
- **#18 Visual separation**: карточка на RichEdit — **значения полей жирным** (`Full name: **Leonard**`), заголовки `── Заметка ──` тоже жирные.
- ПКМ по тексту карточки: меню «Копировать / строка / всё» (починен перехват меню EDIT).

### Changed
- **NOTE** — отдельный блок `── Заметка ──` с переносами; `\n`/`\t` escape.

## [2.4.4] – 2026-07-31

### Changed
- **NOTE** в карточке — отдельный блок: пустая строка, заголовок `── Заметка ──`, текст с переносами ниже.
- Escape в тексте: `\n` → перенос (CRLF в EDIT), `\t` → таб; `\,` `\;` `\\` по RFC.

### Fixed
- (из 2.4.3) iPhone PHOTO base64 не сыплется в текст; фото сверху; base64 `len%4==1`.

## [2.4.3] – 2026-07-28

### Fixed
- **iPhone `\r\r\n`**: корректный split/unfold строк — base64 PHOTO больше не попадает в текст карточки (`John_Doe_IPHONE.vcf`).
- PHOTO: надёжнее собирается base64 (`ENCODING=b`, `;JPEG` без TYPE); orphan-строки base64 не выводятся.
- Скрыты `X-ABADR`; чище ORG/ADR (`\`n`, `;`).
- Фото снова **сверху** карточки.
- Base64: `len%4==1` (битые exports) — отбрасываем лишний символ.

## [2.4.2] – 2026-07-28

### Fixed
- **BlackBerry PHOTO**: JPEG с битой длиной APP1 (перекрывает DQT) — починка маркеров + EOI, GDI+ снова показывает фото (`John_Doe_BLACK_BERRY.vcf`).

## [2.4.1] – 2026-07-28

### Fixed
- Высота текста карточки: точный замер через `EM_POSFROMCHAR` + fallback; если текст короче окна — EDIT **растягивается** на свободное место (больше не обрезается при «пустом» экране, напр. `John_Doe_ANDROID.vcf`).

## [2.4] – 2026-07-28

### Added
- **#12** `pluginst.inf` в архиве — авто-установка плагина в Total Commander.
- **#11** Тёмная тема по **TC dark mode** (`lcp_darkmode` / `ListSendCommand` при `cm_SwitchDarkMode`), не по Windows.
- **#11** Фото **после** текстовых полей, с ограничением размера превью.

### Fixed
- **#13** Карточка: полная высота текста + один внешний вертикальный скролл (без «окна» EDIT со стрелками под фото).
- Apple/iOS vCard: скрыты служебные поля (`X-ADDRESSING-GRAMMAR`, `X-IMAGEHASH`, `PRODID`, …); сохранены кастомные метки (`Домашние контакты`, `День ангела`); без дубля N/FN.

### Build
- Единый архив: `VCFLister-2.4.zip` (`.wlx` + `.wlx64` + `pluginst.inf` + docs).

## [2.3] – 2026-07-28

### Added
- **#8 Быстрый фильтр** над списком контактов (поле «Фильтр...»).
- **Фильтр «📷 фото»** — показывать только контакты с фото.
- **#9 Detect string** для авто-установки TC: `EXT="VCF" | EXT="VCARD"` (ANSI `ListGetDetectString` на x86 и x64).

### Fixed
- **#7 scroll bug**: один **вертикальный** скроллбар справа для всей карточки (фото + полный текст); убран горизонтальный скролл снизу у EDIT; текст переносится.
- Навигация по **отфильтрованному** списку (↑/↓/PgUp/PgDn) с клавиатуры.
- Фокус: стрелки/Enter/Tab из фильтра передают фокус на список; клик по списку забирает фокус у фильтра; ESC из фильтра/чекбокса закрывает Lister.
- Экспорт `ListGetDetectString` для 64-bit TC (раньше только W-версия).

### Build
- Единый архив: `VCFLister-2.3.zip` (`.wlx` + `.wlx64` + docs).
- Бинарники: `VCFLister.wlx64` (x64), `VCFLister.wlx` (Win32).

## [2.2] – 2026-07-28

### Added
- **Resizable splitter** между списком и карточкой (ширина сохраняется в `wincmd.ini` → `ListWidth`).
- **Копирование**: ПКМ по списку (имя/телефон/email/карточка), ПКМ по тексту (выделение/значение строки/всё), **Ctrl+C**.
- **Поиск**: подсветка совпадений в списке + «Найдено: N (k/N)» в статусбаре; выделение в тексте карточки.
- **Tooltip** для длинных имён в списке.
- **Опциональная загрузка PHOTO по URL** (`LoadPhotoUrl=1` в ini; по умолчанию выкл., без сети).
- **vCard 4.0**: GENDER, LANG, KIND, MEMBER, несколько URL; локализация KIND/GENDER.
- Длинные имена в списке: шире колонка + перенос на 2 строки.

### Fixed
- Хвост `=` после имени (QP soft-break vCard 2.1) в списке контактов.
- Правый скроллбар: позиция, диапазон, clamp, место под scrollbar, min-высота EDIT при большом фото.
- Case-insensitive split BEGIN/END:VCARD; клики по статусбару не выбирают контакты.

### Build
- Единый архив: `VCFLister-2.2.zip` (`.wlx` + `.wlx64` + docs).
- Бинарники: `VCFLister.wlx64` (x64), `VCFLister.wlx` (Win32).

## [2.1] – 2026-06-23

### Added
- Статусбар под списком контактов: общее количество + текущая позиция (`Контактов: N (k/N)`).
- Вертикальный скроллбар для правой панели карточки (hRightScroll). Теперь можно прокручивать большое фото вместе с длинным текстом контакта.
- Показ всех контактов, включая полностью пустые (раньше прятались).

### Changed / Improved
- EDIT в правой панели теперь явно имеет `WS_VSCROLL | WS_HSCROLL` — скроллбар появляется когда нужно.
- Фото-URL (PHOTO:https://...) теперь отображаются в текстовом блоке (не скрываются).
- Улучшена обработка `itemN.PHOTO`, `X-FCENCODED-*` и других кастомных полей из vCard 4.0 (в т.ч. примеры из ez-vcard).
- Пустые карточки показываются как "(пустая карточка)" / "(empty card)" в списке.

### Fixed
- Исправлено скрытие PHOTO-URL и утечка base64-данных в текст.
- Синхронизация индексов при показе пустых контактов.
- Мелкие улучшения стабильности скролла и layout'а.

### Build
- Версия поднята до 2.1.
- Бинарники: `VCFLister.wlx64` (x64), `VCFLister.wlx` (Win32).

## [2.0] – 2026-06-18

### Added
- Полноценная поддержка сложных vCard (Apple/macOS, OPPO и др.): X-ABLABEL, X-SOCIALPROFILE, X-ABDATE, X-ABRELATEDNAMES, X-ALTBDAY, IMPP, X-ANDROID-CUSTOM, phonetic имена и т.д.
- Улучшенное отображение: кастомные метки приклеиваются к полям (вместо отдельных "Label: _$!<...>!$_"), очистка лишних `;` в N/ADR, красивый разбор Android Custom Events.
- Пропуск полностью пустых контактов (только BEGIN/END/VERSION) — они больше не попадают в список и не сдвигают индексы.
- Фото выравнивается по левому верхнему краю (top-left) с сохранением пропорций.
- Фокус в поле данных (EDIT) больше не ломает переключение плагинов и режимов просмотра (HEX/PlainText) в Total Commander.

### Fixed
- Проблема с пустыми карточками: теперь синхронно фильтруются и в ParseVCard, и в SplitVCardBlocks → правильные контакты и фото.
- Убрана принудительная полоса прокрутки в панели деталей (WS_VSCROLL/WS_HSCROLL на EDIT). Появляется только когда текст не влезает.
- Улучшена надёжность загрузки фото (восстановлена схема владения HGLOBAL для GDI+ Bitmap, более терпимый Base64Decode + предочистка).
- Мерцание при прокрутке и обновлении фото: WS_CLIPCHILDREN, двойная буферизация в окне фото, InvalidateRect с FALSE.
- Фокус после клика мышью в данные контакта: EditSubclass + ES_NOHIDESEL + возврат фокуса на главное окно плагина.
- Корректная обработка ENCODING=B и BASE64, folded строк v2.1/v3.0, QP, charset.

### Changed
- Рефакторинг: общие утилиты вынесены в vcf_utils.* (убраны дубликаты), удалён мёртвый PhotoDrawer.
- Размер фото увеличен примерно в 2 раза (500 → 1000).
- Улучшена очистка полей (N/ADR без лишних `;`, пропуск пустых FN/N).
- Локализация и BuildFromRawBlock доработаны для кастомных полей (X-*, itemN.*).
- Версия поднята до 2.0 (значительные улучшения отображения, стабильность, UX).

### Build
- Конфигурация: x64 Release → `VCFLister.wlx64`, Win32 Release → `VCFLister.wlx`.
- Проверено в Total Commander 11.54 (Lister) и выше.
- Генерация версии через gen_version.ps1 (git tag + build counter).

## [1.5] – предыдущие версии
(см. git history для деталей 1.x)
